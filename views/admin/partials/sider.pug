.sider
    .inner-menu
        ul
            li
              a(href= prefixAdmin + '/dashboard') Dashboard
            if (role.permissions.includes('order_views'))
              li
                a(href=prefixAdmin+'/orders') Order (#{orderCount})
            if(role.permissions.includes('product_views'))
              li
                a(href= prefixAdmin + '/products') Product
            if(role.permissions.includes('products-category_views'))
              li
                a(href= prefixAdmin + '/categories') Category 
            if(role.permissions.includes('role_views'))
              li
                a(href= prefixAdmin + '/roles') Role
            if(role.permissions.includes('role_permission'))
              li
                a(href= prefixAdmin + '/roles/permissions') Permission
            if(role.permissions.includes('account_views'))
            li
              a(href= prefixAdmin + '/accounts') Accounts
            li 
              a(href=prefixAdmin + '/bin') Recycle Bin
            li
              a(href=prefixAdmin+'/settings/general') Settings 
